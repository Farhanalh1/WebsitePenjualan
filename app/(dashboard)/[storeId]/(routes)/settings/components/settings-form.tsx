'use client'

import { Store } from "@prisma/client"

interface SettingsFormProps {
    initialData: Store
}

export const Settingsform = () => {
    return (
        <div>
            Ini Settings Form
        </div>
    )
}